# ForScore data files (personal, large — never commit)
*.4sb
*.4sc
*.4ss

# Extraction output (dir in main repo; may be a symlink in a worktree)
/out
/out/
/extracted/

# Agent / editor environment injections (mounted as character devices)
.claude/
.idea
.vscode
.mcp.json
.bash_profile
.bashrc
.gitconfig
.gitmodules
.profile
.ripgreprc
.zprofile
.zshrc

# Git worktrees (isolated workspaces)
/.worktrees/

# Vendored front-end libs (large; re-fetch via web/vendor/README.md)
/web/vendor/webgazer.js

# Node / front-end tooling
web/node_modules/

# Python
__pycache__/
*.pyc
.venv/

# Test fixtures downloaded on demand (public-domain scores; never committed)
/tests/fixtures/
